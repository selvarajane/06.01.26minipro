{
  "buildCommand": "cd sample && npm install && npm run build",
  "outputDirectory": "sample/dist",
  "installCommand": "cd sample && npm install",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}

